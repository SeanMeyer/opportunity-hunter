# Unraid deployment — September 9, 2026

- Release: `v0.3.0`, application commit `e9c38f5` on main.
- Image: `ghcr.io/seanmeyer/opportunity-hunter:v0.3.0`.
- Published digest: `sha256:86a33a09c2496e8a5bb3390fb09ac52b5cafdb8b28847445b46809a2c37f87e0`.
- Host/container: `tower` / `opportunity-hunter`.
- Dashboard: `http://tower:8085` (container port 8080).
- Existing data mount and environment settings were preserved. Host port 8080
  belongs to another service, so the saved Unraid template now maps 8085.
- Restart policy: `unless-stopped`; Unraid autostart enabled.

The previous v0.2.1 container had been stopped for four months. It remains
stopped as `opportunity-hunter-rollback-v021`. Protected backup directory:
`/mnt/user/appdata/opportunity-hunter-backups/20260909T215806Z`.
It contains the original database, container configuration, environment and
Unraid template; do not publish these configuration files because they contain
credentials. The deployment helper also lives there.

The release workflow passed. Before switching, the new image migrated a copied
database with networking disabled, served its status page, passed SQLite
integrity checking and preserved all 302 opportunities, 270 evaluations,
zero feedback entries and two preference records. Production then passed its
own integrity check, reported version v0.3.0, served all four hunt pages, and
remained running with zero restarts. Existing Gemini credentials work and new
evaluations were observed. Initial scans were still processing at handoff.

Observed configuration limitation: Google Routes API returns HTTP 403 for new
travel-time lookups. Cached distances remain available. This does not establish
which Google Cloud permission, API restriction or billing setting needs changing.
No credential or Google Cloud setting was changed during deployment.

Rollback requires stopping the replacement before starting the retained old
container. The old container still has its obsolete host-port 8080 mapping;
recreate it on 8085 using the protected original configuration if rollback is
needed. Keep a fresh copy of the current database before any restoration, since
the replacement may have added evaluations or user feedback after deployment.

## Routes and scan monitoring follow-up

The Routes failure was `API_KEY_SERVICE_BLOCKED`: the production key allowed
Gemini but did not allow Routes. Routes was already enabled and billing was
active. The key restriction now additionally permits only
`google.maps.routing.v2.Routes.ComputeRouteMatrix`. The existing Gemini target
was retained. Walking and driving requests from Tower both returned
`ROUTE_EXISTS`, and the application subsequently wrote a fresh distance cache
entry without restarting.

The status display fix distinguishes running scans from completed counts, and
Routes diagnostics now handle Google's array-shaped error envelope while
redacting the configured key. Regression tests and the full Go test/vet suite
passed; desktop and mobile running states were inspected in a synthetic preview.

Production checks also found an invalid comedy Discord webhook (read-only GET
returned 401 / Invalid Webhook Token); the powder webhook returned 200. The
initial comedy scan saved 28 evaluations but stalled during delivery. Pending
messages must be retained until the comedy webhook is replaced. Do not send
additional test messages to diagnose this credential problem.

A thread heartbeat checks production every 30 minutes until September 10,
2026 at 22:30 UTC, including scan progress, Routes, delivery state, dashboard,
and status filters. It then summarizes and pauses. It does not trigger extra
AI runs or test Discord messages.

### v0.3.2 deployed

- Application commit: `c0fdbda`; release workflow `34413638260` succeeded.
- Image digest: `sha256:caee89081d589c2f2b78c6d439ad154902091caaec596dea624421d088748b51`.
- Fresh backup: `/mnt/user/appdata/opportunity-hunter-backups/20260909T224434Z`.
- Retained rollback container: `opportunity-hunter-rollback-v030` (stopped).
- Copied-database isolated HTTP/integrity smoke passed before replacement.
  Environment, data mount, 8085 mapping, restart policy and Unraid template
  were preserved. The interrupted old run was explicitly closed after stopping
  its process; evaluations and pending deliveries were retained.
- Delivery failures now stop further attempts for that hunt run, while scans
  continue. Long Discord Retry-After values defer delivery promptly. Generated
  briefings remain paired with their queued evaluations across retries.
- Fresh full Go tests and vet passed, with independent review of the fixes.
- Live v0.3.2 reported zero restarts and SQLite quick_check `ok`. All four hunt
  pages, status, and filtered status returned HTTP 200. Browser checks confirmed
  running-state text, history filtering, and the comedy error in the log filter.
- Comedy immediately reported its invalid webhook and allowed performing arts
  to begin. There were 28 saved comedy and three new performing-arts evaluations
  at verification. Twenty-five comedy deliveries remain pending; three
  zero-action deliveries were completed. Performing arts was still running;
  movies and powder had not yet completed their initial scans. Continued
  monitoring must verify those runs and schedule advancement rather than infer
  success from healthy HTTP responses.

### v0.3.3: corrected schedule verification and comedy opt-out

The user's report exposed a gap in the earlier verification: comedy's completed
run still showed an April Next date while later startup hunts were running.
HTTP health and status-filter checks had not validated that date. Startup now
advances each finished hunt before starting the next, including cancellation
handling. Overdue UI schedules say Scan due; failed runs link to System Status;
new-opportunity counts are labeled accurately instead of total scanned items.

The user explicitly disabled comedy Discord. Its webhook was cleared in the
runtime and saved Unraid template. Opted-out pending deliveries are completed
without sends or notification counts; a dry run still retains them. Existing
recommendation cards and evaluation data were preserved.

- Application commit: `6c25a2f`; release `v0.3.3`; workflow `34415485497` passed.
- Image digest: `sha256:9d47376d7717dd9ffa191275916d356282ede0c2b2284a9d9a2ce5527452e765`.
- Backup: `/mnt/user/appdata/opportunity-hunter-backups/20260909T230820Z`.
- Rollback container: `opportunity-hunter-rollback-v032`, stopped.
- Fresh full tests (`go test ./... -count=1 -timeout=90s`), vet, focused review,
  and copied-database deployment smoke passed.
- Live verification: comedy, performing arts and movies each completed OK with
  zero new items and zero notifications. Comedy's queue is empty, its webhook
  is absent in runtime/template, and its 63 cards remain. The exact comedy page
  was browsed and captured on desktop and at 390px mobile width with no horizontal
  overflow. Banner: Last scan / 0 new opportunities / Next Sun Sep 13 6:00 AM /
  Discord notifications off. Database quick_check passed; zero container restarts.

Separate unresolved monitoring issue: powder reevaluated legacy April rows
before expiry, then a downgrade PostMessage failed because the Discord webhook
points to a forum and requires a thread name or ID (code 220001). Its latest run
still reports error. The heartbeat now explicitly tracks this issue, expiry and
obsolete pending updates. Do not describe all hunts as healthy. Historical
notification totals written before v0.3.3 may include actions skipped because
no webhook was configured; new runs use the corrected accounting.

### Current-card and notification audit

The 63 visible comedy cards all had future stored dates at the September 9
audit; that count included duplicate source listings. The reconciled copied
database has 58 cards, each with an upcoming date. Two reconciliation passes
produce the same count and schedules. Performing arts' 28 visible cards also
have future dates. Movie release dates describe availability, not showtimes.

The application now checks expiry when loading cards, before evaluation, and
before delivery. Multi-date cards and evaluator inputs contain remaining dates
in local time; date-only listings stay available through their calendar day.
Expired and superseded queued opportunities cannot send stale recommendations.
Powder forum updates target a thread. Comedy Discord remains disabled.

Comedy reconciliation retains source aliases, picks, and feedback. It matches
provider identity or overlapping local dates with the same normalized title
and verified venue; distinct venues remain separate. Historical multi-date
records are unioned conservatively because their representative source ID does
not establish provenance for every date. New partial source observations must
not erase another provider's dates. Alias lookup is indexed.

The one-time transactional `scripts/repair-comedy-20260909.py` corrects two
independently audited records, with identity assertions and history-count checks:

- Sam Jay: replace obsolete November dates on canonical row 40 with row 344's
  January 22–23, 2027 schedule, confirmed by the
  [current ticket page](https://www.ticketweb.com/event/sam-jay-denver-improv-tickets/14740653).
- Hasan Minhaj/Ronny Chieng: alias row 312 to 311; their timestamps are the same
  instant and the [official event](https://www.axs.com/events/1479450/hasan-hates-ronny-ronny-hates-hasan-tickets)
  confirms Red Rocks, September 24, 2026 at 7:30 PM.

Apply this script only after stopping the daemon, taking a fresh SQLite backup,
and installing the additive schema. It passed twice on a copied production DB.
No opportunity, pick, or feedback rows are deleted. Old binaries can read the
additive schema, but ignore supersession and may show duplicate historical cards.

UI checks covered all hunt pages, status/history filters, mobile and desktop,
empty/error states, expanded details, and preferences/schedules. Copied-DB
interaction checks saved thumbs up and down with notes, switched votes,
cancelled draft changes, saved preferences and a schedule, and exercised sort
and score filters. Production feedback and schedules were not changed by QA.
Cards expose assessment dates, suppress old urgency, show comedy event years, and keep
long booking notes in details. TMDB cards label release dates and link to movie
details. Status shows sanitized error summaries.

Full Go tests and vet passed; after the final small pick-order/year-label
changes, storage, comedy and web tests passed again, as did vet. Focused review
and Gemini review informed the fixes. Gemini's remaining broad venue matching
suggestion was intentionally not applied: fuzzy addresses can merge distinct
venues. Supersession writes flatten chains, and UI dates are normalized before
card rendering. No extra live AI evaluations or test Discord sends were run.

#### Held-delivery operations

A queued delivery with changed dates, or a partially prepared batch containing
obsolete picks, is retained with an explicit status error rather than replaying
stale generated prose or duplicating a completed action. Inspect its saved
evaluation, current opportunity dates, and action checkpoints before intervening.
Do not clear checkpoints or blindly replay the batch. Wholly obsolete batches
are completed without sends automatically. A still-current held batch needs an
audited cancellation/replacement decision; there is no cancel button yet.

### v0.3.5 deployed and verified

- Application commit `f371e30`; successful release workflow `34422261810`.
  The superseded v0.3.4 build was cancelled before deployment to include the
  final canonical-pick ordering and year-label fixes.
- Image digest `sha256:50fd6b0643761a15898e5ef010cb7bf90741f30c53d0fb33ad87ea66ee8778e6`.
- Backup `/mnt/user/appdata/opportunity-hunter-backups/20260910T003729Z`
  (directory timestamp uses the host clock; see clock issue below).
  Previous container `opportunity-hunter-rollback-v033` is retained, stopped.
- Isolated copied-DB HTTP/integrity smoke passed, followed by a stopped fresh
  backup, network-isolated web-only schema migration, and audited data repair.
  Pre-daemon rollback restores the backup; after any attempted daemon start,
  failure preserves delivery checkpoints and leaves containers stopped for
  recovery instead of replaying notifications from an older backup.
- All four startup hunts finished OK. Comedy found three new opportunities,
  evaluated three groups, added one visible pick, and notified zero. The other
  hunts recorded zero new/evaluated/notified. These were ordinary startup runs,
  not manually triggered AI tests. The 17 obsolete powder deliveries cleared;
  no pending deliveries remain and no notifications were sent.
- Live comedy now has 59 visible cards, all with upcoming dates even allowing
  for the host clock error. Performing arts has 28, movies 4, powder 0.
  All hunt pages and status return HTTP 200 and were browsed. Comedy's banner
  shows 3 new opportunities, Next Sun Sep 13 at 6 AM, and notifications off.
  Status agrees with persisted runs and schedules. Screenshots captured live
  comedy and status; desktop/mobile and interaction evidence also uses the
  copied DB to avoid modifying production preferences or feedback.
- SQLite quick_check is OK; zero container restarts. No opportunity, pick,
  evaluation, or feedback history was deleted. Counts increased only through
  the ordinary startup scan. Runtime and saved comedy opt-out remain intact.
- Production monitoring remains active every 30 minutes through September 10
  at 22:30 UTC, including date guards, schedules, Routes and delivery queues.

Separate host issue: Unraid's UTC clock was about 10 minutes 10 seconds behind
the workstation. `USE_NTP="no"`, no ntpd process, and ntpq refused connection.
Changing host time affects all containers, so approval was requested separately;
the host clock was left unchanged pending the user's answer. This limits exact
expiry timing until corrected. No currently visible comedy card was close to
that boundary.

Host clock resolved with user approval: on September 10 around 00:58 UTC,
backed up ident.cfg and ntp.conf under
`/mnt/user/appdata/opportunity-hunter-backups/time-sync-20260910T004751Z`
(pre-correction clock), changed the persisted USE_NTP setting to yes, and
started Unraid's native `/etc/rc.d/rc.ntpd` service using the existing
pool.ntp.org setting. ntpq reported sync_ntp, leap_none, an external selected
peer, and a system offset around -4.6 ms. Host UTC agreed with the workstation
after allowing for SSH latency. The app remained running with zero restarts,
status returned HTTP 200, and all next scan times remained in the future.
Monitoring instructions now include checking that NTP stays synchronized.

### September 10, 01:51 UTC monitoring follow-up

v0.3.5 remained running with zero restarts, no pending deliveries, SQLite
quick_check OK, 59 upcoming comedy cards, future schedules, and comedy Discord
disabled. NTP had selected an external peer with millisecond-scale offset.
The walking Routes cache gained a 31st entry without an authorization error.
The isolated source trial remained at two cycles, last at September 9 21:59
UTC; its six-hour interval had not elapsed, so no extra cycle was run.

The Alaska warning reproduced with public NWS requests: AER's AFD product list
was empty, while AFC had 14 current products. The
[official Anchorage discussion](https://www.weather.gov/arh/fire_afd) uses
AFDAFC for Southcentral and Southwest Alaska. FetchAFD now maps AER/ALU grid
identifiers to AFC only for discussion products. Forecast grid identifiers
remain unchanged. A real read-only FetchAFD(AER) returned AFC's September 10
00:05 UTC discussion (6,920 bytes), without AI or notifications.

The status page's powder warning filter also hid this warning: it had a saved
run ID but no explicit hunt attribute. RecentLogs now falls back to the
associated run's hunt, preserving explicit attributes and unassociated global
logs. Both defects have failed-first regression tests. Powder/weather, storage
and web suites passed, as did vet. Focused review found no actionable defect.

v0.3.6 deployed from `c703436`; release workflow `34427729666` passed.
Full Go tests passed as well. Gemini confirmed the scoped behavior; its log
query scaling concern was checked against production (321 logs, about 1.8 ms).
Future normalization/refactoring suggestions were kept outside this narrow fix.

- Digest `sha256:ec70d0d484b04c19cd5357f045f7218abc1a3e205855fe27a45428a6b3fe416c`.
- Backup `/mnt/user/appdata/opportunity-hunter-backups/20260910T021146Z`;
  retained stopped rollback container `opportunity-hunter-rollback-v035`.
- Copied-database isolated smoke passed before the backed-up replacement.
- The ordinary 20:00 scheduled powder run on v0.3.5 completed and advanced its
  schedule but repeated the Alaska warning. After v0.3.6 deployment, all four
  startup hunts completed OK at 20:12 local time with zero WARN/ERROR logs,
  zero new opportunities, zero evaluations and zero notifications.
- No pending deliveries; SQLite quick_check OK; zero container restarts.
  Comedy Discord remains off. All hunt pages and filtered status return 200.
- Browser verification of `/status?hunt=powder&level=WARN` now shows the four
  historical NWS warnings attributed to powder. They are retained history;
  the new run has none. Monitoring continues for recurring source failures.

September 10, 02:44 UTC check: no newer runs or evaluations since the clean
v0.3.6 startup, no pending/running work, zero restarts, and NTP offset about
+2.9 ms. Browser status agrees with stored future schedules and shows only the
four historical powder warnings. Comedy Discord is off; powder is configured.
All 57 comedy and 28 performing-arts cards have upcoming dates. The change from
59 comedy cards is explained by reconciliation during the 20:12 startup:
Nikki Glaser row 294 now aliases 34, and Daniel Sloss row 325 aliases 37 after
their refreshed schedules overlapped. Both canonical cards retain the combined
show dates. Comparison with the pre-deployment backup confirms unchanged counts
of opportunities (598), picks (382), evaluations (356), and feedback (0).
The next isolated source-trial cycle is not due before 03:59 UTC; none was run.

September 10, 14:29 UTC check (actual execution time, later than the queued
heartbeat timestamp): production remained on v0.3.6 with zero restarts, no
new WARN/ERROR logs, no pending/running work, unchanged evaluations and Routes
cache, and SQLite quick_check OK. NTP was synchronized with about +0.8 ms
offset. Browser dashboard/status filters agreed with stored runs, future
schedules, 57 current comedy cards and the comedy notification opt-out.
All 28 performing-arts cards also retained upcoming dates.

Isolated source-trial cycle 3 ran at 14:29:43 UTC after the six-hour gate:
3.5 seconds, no hunt errors, no new opportunities, zero AI calls and zero
external notifications. Five NWS requests and one Open-Meteo request returned
HTTP 200. The copied trial DB reported 80 discovered comedy rows and zero
powder rows. This checks repeat discovery and Colorado I-70 weather access,
not fresh AI recommendation quality or live Discord delivery. Cycle 4 must
wait until at least 20:29:43 UTC. Production was not manually scanned.

September 10, 20:49 UTC: isolated source-trial cycle 4 completed at
20:49:25 UTC, more than six hours after cycle 3. It took 2.8 seconds and found
two new comedy items (82 discovered rows now stored in the copied trial DB).
Powder found zero new items. Both hunts reported no errors; all five NWS
requests and the Open-Meteo request returned HTTP 200. There were zero AI calls
and zero external notifications. This completes the four-cycle source trial;
it demonstrates repeat discovery and Colorado I-70 weather access, not fresh
AI recommendation quality, coverage of every hunt/source, or live delivery.

Production remained on v0.3.6 with zero restarts, no new WARN/ERROR logs,
no running scans or pending deliveries, unchanged evaluations and Routes cache,
and SQLite quick_check OK. No production scans have occurred since startup.
Browser dashboard/status values matched persisted results and future schedules;
57 comedy and 28 performing-arts cards retained upcoming dates. Comedy Discord
remains off. NTP was synchronized with approximately -3.3 ms offset.
