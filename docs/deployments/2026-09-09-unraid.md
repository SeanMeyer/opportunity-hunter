# Unraid deployment — September 9, 2026

- Release: `v0.3.0`, application commit `e9c38f5` on main.
- Image: `ghcr.io/seanmeyer/opportunity-hunter:v0.3.0`.
- Published digest: `sha256:86a33a09c2496e8a5bb3390fb09ac52b5cafdb8b28847445b46809a2c37f87e0`.
- Host/container: `tower` / `opportunity-hunter`.
- Dashboard: `http://tower:8085` (container port 8080).
- Existing data mount and environment settings were preserved. Host port 8080
  belongs to another service, so the saved Unraid template now maps 8085.
- Restart policy: `unless-stopped`; Unraid autostart enabled.

The previous v0.2.1 container had been stopped for four months. It remains
stopped as `opportunity-hunter-rollback-v021`. Protected backup directory:
`/mnt/user/appdata/opportunity-hunter-backups/20260909T215806Z`.
It contains the original database, container configuration, environment and
Unraid template; do not publish these configuration files because they contain
credentials. The deployment helper also lives there.

The release workflow passed. Before switching, the new image migrated a copied
database with networking disabled, served its status page, passed SQLite
integrity checking and preserved all 302 opportunities, 270 evaluations,
zero feedback entries and two preference records. Production then passed its
own integrity check, reported version v0.3.0, served all four hunt pages, and
remained running with zero restarts. Existing Gemini credentials work and new
evaluations were observed. Initial scans were still processing at handoff.

Observed configuration limitation: Google Routes API returns HTTP 403 for new
travel-time lookups. Cached distances remain available. This does not establish
which Google Cloud permission, API restriction or billing setting needs changing.
No credential or Google Cloud setting was changed during deployment.

Rollback requires stopping the replacement before starting the retained old
container. The old container still has its obsolete host-port 8080 mapping;
recreate it on 8085 using the protected original configuration if rollback is
needed. Keep a fresh copy of the current database before any restoration, since
the replacement may have added evaluations or user feedback after deployment.
